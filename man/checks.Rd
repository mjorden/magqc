% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checks.R
\name{checks}
\alias{checks}
\alias{check_sample_interval}
\alias{check_along_line_spacing}
\alias{check_line_deviation}
\alias{check_line_separation}
\alias{check_clearance}
\alias{check_spikes}
\alias{check_fourth_difference}
\alias{check_diurnal}
\alias{check_crossovers}
\alias{check_heading_error}
\title{QC checks}
\usage{
check_sample_interval(survey, spec)

check_along_line_spacing(survey, spec)

check_line_deviation(survey, spec, plan = NULL)

check_line_separation(survey, spec)

check_clearance(survey, spec)

check_spikes(survey, spec)

check_fourth_difference(survey, spec)

check_diurnal(base, spec, survey = NULL)

check_crossovers(xo, survey, spec)

check_heading_error(xo, survey, spec)
}
\arguments{
\item{survey}{A \code{magqc_survey} from \code{\link[=as_survey]{as_survey()}}.}

\item{spec}{A \code{\link[=survey_spec]{survey_spec()}}.}

\item{plan}{Optional flight plan: a data frame with columns \code{line}, \code{x0},
\code{y0}, \code{x1}, \code{y1} giving the planned start and end of each line.}

\item{base}{Base-station record: a data frame with \code{time} (POSIXct) and
\code{mag_base} (nT).}

\item{xo}{Crossover table from \code{\link[=crossovers]{crossovers()}}.}
}
\description{
Each \verb{check_*()} evaluates one acceptance criterion from a
\code{\link[=survey_spec]{survey_spec()}} against a \code{\link[=as_survey]{as_survey()}} table and returns a tibble of
flagged intervals (see Details). \code{\link[=run_qc]{run_qc()}} runs them all; call them
individually to re-test one criterion with a different threshold.
}
\details{
The returned tibble has one row per flagged interval with columns \code{check},
\code{line}, \code{i_start}, \code{i_end} (row indices into the survey), \code{fid_start},
\code{fid_end}, \code{time_start}, \code{time_end}, \code{x}, \code{y}, \code{lon}, \code{lat} (interval
midpoint), \code{value}, \code{threshold}, \code{units} and \code{description}. It carries a
\code{metric} attribute holding the summary statistic reported on the scorecard.
\itemize{
\item \code{check_sample_interval()} - breaks in the data stream longer than
\code{max_gap} seconds.
\item \code{check_along_line_spacing()} - ground distance between consecutive
samples exceeding \code{max_along_line_sep}, excluding samples on either side
of a gap (those are reported by the gap check).
\item \code{check_line_deviation()} - lateral departure from the planned line, or
from a total-least-squares fit through the flown line when no \code{plan} is
given. Without a plan a whole line flown parallel to but off its planned
position is invisible to this check and is caught by
\code{check_line_separation()} instead.
\item \code{check_line_separation()} - separation between adjacent traverse lines,
measured perpendicular to \code{line_azimuth}, against \code{line_spacing} +/-
\code{line_spacing_tol}.
\item \code{check_clearance()} - radar altimeter against \code{nominal_clearance} +/-
\code{clearance_tol}.
\item \code{check_spikes()} - samples whose fourth difference exceeds \code{spike_nsigma}
running MADs of the fourth difference. The test is made on the fourth
difference rather than the field itself because a running-median filter
on a smooth, steep anomaly reads ordinary noise as outliers; the fourth
difference removes the geology first.
\item \code{check_fourth_difference()} - the fourth-difference noise envelope on
the despiked field (see \code{\link[=fourth_difference]{fourth_difference()}}). Both noise tests are
evaluated on each continuous stretch of a line separately: a data gap
breaks the series, so the field jump across it is not mistaken for a
spike.
\item \code{check_diurnal()} - base-station departure from a linear chord over
\code{diurnal_window} seconds; survey samples acquired inside an exceedance
window are flagged.
\item \code{check_crossovers()} - traverse/tie misfit at each intersection (see
\code{\link[=crossovers]{crossovers()}}); RMS misfit is the metric, individual intersections above
\code{max_crossover_abs} are flagged.
\item \code{check_heading_error()} - difference in mean crossover misfit between
traverses flown in opposite directions.
}
}
