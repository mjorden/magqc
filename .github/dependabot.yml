version: 2
updates:
  # Keeps the SHA-pinned actions in .github/workflows current by PR.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      actions:
        patterns: ["*"]
