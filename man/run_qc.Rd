% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc.R
\name{run_qc}
\alias{run_qc}
\title{Run every QC check on a survey}
\usage{
run_qc(
  survey,
  spec = survey_spec(),
  base = NULL,
  plan = NULL,
  levelling = TRUE,
  levelling_order = c("linear", "constant"),
  igrf = TRUE,
  grid = TRUE,
  rtp = TRUE
)
}
\arguments{
\item{survey}{A \code{magqc_survey} from \code{\link[=as_survey]{as_survey()}}, a plain data frame that
\code{\link[=as_survey]{as_survey()}} accepts, or a \code{magqc_sim} from \code{\link[=sim_survey]{sim_survey()}} (in which case
\code{base} and \code{spec} default to the simulation's own).}

\item{spec}{A \code{\link[=survey_spec]{survey_spec()}}.}

\item{base}{Optional base-station record (\code{time}, \code{mag_base}). When given,
\code{mag} is set to \code{mag_raw} minus the base-station departure from its
median level before any field statistic is computed.}

\item{plan}{Optional flight plan; see \code{\link[=check_line_deviation]{check_line_deviation()}}.}

\item{levelling}{Run tie-line levelling (\code{\link[=level_ties]{level_ties()}}) after the checks
and report the post-levelling residual on the scorecard? Skipped
silently when there are no crossovers.}

\item{levelling_order}{Passed to \code{\link[=level_ties]{level_ties()}} as \code{order}.}

\item{igrf}{Remove the IGRF main field (\code{\link[=igrf_field]{igrf_field()}})? Adds \code{mag_igrf}
and \code{mag_res} (levelled field minus IGRF) to the survey; skipped
silently without longitude/latitude.}

\item{grid}{Grid the field with \code{\link[=grid_field]{grid_field()}} for the map layer and the
gridded-field panel? Grids the residual when the IGRF was removed,
otherwise the levelled field. Cell size is a quarter of the traverse
spacing, blanking distance half of it, and samples flagged as spikes
are left out.}

\item{rtp}{Reduce the residual grid to the pole (\code{\link[=reduce_to_pole]{reduce_to_pole()}})
with the IGRF inclination and declination? Needs both \code{igrf} and
\code{grid}; skipped silently otherwise.}
}
\value{
A list of class \code{magqc_result} with elements \code{survey} (with a
\code{mag_lev} column when levelling ran), \code{base}, \code{spec}, \code{flags},
\code{scorecard}, \code{lines}, \code{stats}, \code{crossovers}, \code{diurnal} (base-station
statistics), \code{fourth_difference} (per-sample series), \code{heading} (misfit
by flight direction), \code{levelling} (a \code{magqc_levelling}, or \code{NULL}),
\code{igrf} (model, epoch, altitude and \code{F}/\code{I}/\code{D} at the block centre, or
\code{NULL}), \code{grid} (a \code{magqc_grid}, or \code{NULL}) and \code{rtp} (the grid reduced
to the pole, or \code{NULL}).
}
\description{
Applies the diurnal correction (when a base-station record is supplied),
computes the crossover table, runs each \verb{check_*()} from the
\link{checks} family and assembles the flags, a per-check scorecard, per-line
statistics and survey totals into a \code{magqc_result} that \code{\link[=qc_report]{qc_report()}}
renders.
}
\examples{
res <- run_qc(sim_survey(seed = 3))
res$scorecard
res$levelling
}
