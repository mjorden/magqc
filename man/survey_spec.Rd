% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec.R
\name{survey_spec}
\alias{survey_spec}
\title{Define a survey acceptance specification}
\usage{
survey_spec(
  line_spacing = 200,
  tie_spacing = 2000,
  line_azimuth = 0,
  tie_azimuth = 90,
  sample_rate = 10,
  ground_speed = 60,
  max_along_line_sep = NULL,
  line_spacing_tol = 0.1,
  max_deviation = 25,
  nominal_clearance = 60,
  clearance_tol = 15,
  fourth_diff_tol = 0.05,
  fourth_diff_normalize = TRUE,
  spike_nsigma = 6,
  spike_window = 21,
  max_diurnal_dev = 3,
  diurnal_window = 60,
  max_crossover_rms = 2,
  max_crossover_abs = 6,
  max_heading_error = 2,
  max_gap = 1,
  max_levelled_rms = 1,
  max_levelled_abs = 3
)
}
\arguments{
\item{line_spacing}{Nominal separation between adjacent traverse lines, m.}

\item{tie_spacing}{Nominal separation between tie (control) lines, m.}

\item{line_azimuth}{Planned traverse heading, degrees clockwise from north.}

\item{tie_azimuth}{Planned tie-line heading, degrees clockwise from north.}

\item{sample_rate}{Acquisition rate of the magnetometer, Hz.}

\item{ground_speed}{Nominal survey ground speed, m/s.}

\item{max_along_line_sep}{Maximum accepted ground distance between
consecutive samples, m. Defaults to 1.5x the nominal
\code{ground_speed / sample_rate}, which tolerates a modest tailwind.}

\item{line_spacing_tol}{Fractional tolerance on \code{line_spacing}; 0.10 accepts
an actual separation within +/-10\% of nominal.}

\item{max_deviation}{Maximum lateral departure from the planned (or, absent
a plan, the best-fit) line, m.}

\item{nominal_clearance}{Planned terrain clearance (radar altimeter), m.}

\item{clearance_tol}{Accepted departure from \code{nominal_clearance}, m.}

\item{fourth_diff_tol}{Fourth-difference noise envelope, nT. Interpreted on
the normalised operator - see \code{\link[=fourth_difference]{fourth_difference()}}.}

\item{fourth_diff_normalize}{Whether the fourth-difference operator is
divided by 16 before comparison with \code{fourth_diff_tol}.}

\item{spike_nsigma}{Robust z-score of the fourth difference above which a
sample is called a spike.}

\item{spike_window}{Window length, in samples, for the running MAD that
scales the fourth difference in the spike test. Must be odd.}

\item{max_diurnal_dev}{Maximum departure of the base-station field from a
linear chord fitted over \code{diurnal_window}, nT. This is the usual form of
the diurnal specification: it constrains curvature and short-period
activity rather than the total drift over the flight.}

\item{diurnal_window}{Chord length for the diurnal test, seconds.}

\item{max_crossover_rms}{Maximum RMS misfit at traverse/tie intersections
before levelling, nT.}

\item{max_crossover_abs}{Per-intersection misfit above which a single
crossover is flagged, nT.}

\item{max_heading_error}{Maximum difference in mean crossover misfit between
opposing flight directions, nT.}

\item{max_gap}{Longest accepted break in the data stream, seconds.}

\item{max_levelled_rms}{Maximum RMS crossover misfit \emph{after} tie-line
levelling, nT (see \code{\link[=level_ties]{level_ties()}}).}

\item{max_levelled_abs}{Per-crossover misfit after levelling above which
the crossing is flagged, nT. A crossing that a low-order per-line
correction cannot reconcile points at a real problem - heading error,
lag, or a bad tie - rather than ordinary line-to-line drift.}
}
\value{
An object of class \code{magqc_spec}.
}
\description{
Every QC check in this package reads its threshold from a \code{magqc_spec}
object. The defaults describe a typical fixed-wing high-resolution
magnetic survey (200 m traverse spacing, 60 m nominal terrain clearance,
10 Hz acquisition); they are a starting point, not a standard. Real
acceptance criteria come from the contract for the survey being flown, so
override anything that does not match yours.
}
\examples{
spec <- survey_spec(line_spacing = 100, nominal_clearance = 40)
spec$max_along_line_sep
}
