% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grid.R
\name{grid_field}
\alias{grid_field}
\title{Grid the field}
\usage{
grid_field(
  x,
  channel = NULL,
  cell = NULL,
  method = c("mincurv", "idw"),
  blank_distance = NULL,
  exclude = NULL,
  max_iter = 2000L,
  tol = 0.01
)
}
\arguments{
\item{x}{A \code{magqc_result} (cell and blanking default from its spec) or a
\code{magqc_survey} (then \code{cell} is required).}

\item{channel}{Field column to grid. Defaults to the residual \code{mag_res}
when the IGRF was removed, else \code{mag_lev} when levelling ran, else
\code{mag}.}

\item{cell}{Grid cell size, m. Defaults to a quarter of the traverse
spacing, the usual choice for line data.}

\item{method}{\code{"mincurv"} (default) or \code{"idw"}.}

\item{blank_distance}{Nodes farther than this (Chebyshev distance, m)
from any sample are \code{NA}. Defaults to half the traverse spacing, which
keeps every node between adjacent lines and blanks beyond the block.}

\item{exclude}{Row indices of samples to leave out. For a \code{magqc_result}
this defaults to the samples flagged as spikes - a known point defect
is not field, and a minimum-curvature surface would otherwise ring
around each one.}

\item{max_iter, tol}{Relaxation stops when the largest change in a sweep
falls below \code{tol} nT or after \code{max_iter} sweeps at the finest level.}
}
\value{
An object of class \code{magqc_grid}: \code{x}, \code{y} (node coordinates, m),
\code{z} (matrix \code{length(x)} by \code{length(y)}, nT, \code{NA} where blanked),
\code{cell}, \code{channel}, \code{method}, \code{blank_distance}, \code{n_data}, \code{n_excluded}, \code{n_nodes},
\code{n_blank}, \code{rms_fit} (RMS of sample minus grid interpolated at the
sample), \code{iterations}, and \code{bounds} (west/east/south/north in degrees)
when the survey has longitude and latitude.
}
\description{
Interpolates a field channel onto a regular grid in the survey's local
coordinates. The default method is minimum curvature (Briggs 1974): the
samples are binned to the nearest node, and the free nodes are relaxed to
the biharmonic solution - the smoothest surface through the data - with a
coarse-to-fine start so the relaxation converges in a few hundred sweeps.
\code{"idw"} is an inverse-distance alternative. Nodes farther than
\code{blank_distance} from any sample are blanked, so the map shows the field
only where it was flown.
}
\details{
Up to 40,000 nodes the minimum-curvature system is solved exactly (sparse
LU); larger grids fall back to Jacobi relaxation from a coarse-to-fine
start, bounded by \code{max_iter} and \code{tol}.
}
\examples{
res <- run_qc(sim_survey(seed = 4), grid = FALSE)
g <- grid_field(res)
g
}
\references{
Briggs, I. C. (1974). Machine contouring using minimum
curvature. Geophysics, 39(1), 39-48.
}
