% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/levelling.R
\name{level_ties}
\alias{level_ties}
\title{Tie-line levelling}
\usage{
level_ties(
  x,
  xo = NULL,
  order = c("linear", "constant"),
  min_crossovers_linear = 3
)
}
\arguments{
\item{x}{A \code{magqc_survey} or a \code{magqc_result} (whose crossover table is
reused).}

\item{xo}{Crossover table from \code{\link[=crossovers]{crossovers()}}; computed when \code{NULL}.}

\item{order}{\code{"linear"} (default) or \code{"constant"}.}

\item{min_crossovers_linear}{Crossings a traverse needs before a drift
term is fitted.}
}
\value{
An object of class \code{magqc_levelling}: a list with \code{coefficients}
(one row per traverse: \code{line}, \code{order}, \code{n_crossovers}, \code{c0} nT, \code{c1}
nT/m), \code{crossovers} (the input table plus \code{along_traverse},
\code{misfit_before}, \code{misfit_after}), \code{rms} (\code{c(before, after)}), \code{mag_lev}
(levelled field aligned with the survey rows) and \code{order}.
}
\description{
Removes the between-line component of the crossover misfit by fitting a
low-order correction to each traverse line and subtracting it, holding
the tie lines fixed. With \code{order = "constant"} each traverse gets a single
offset (the mean of its crossover misfits); with \code{order = "linear"} a
traverse with at least \code{min_crossovers_linear} crossings gets an offset
plus a drift term in distance along the line, and the others fall back to
a constant. Lines with no crossovers are left untouched.
}
\details{
The levelled channel is a separate column, \code{mag_lev}, so every noise and
geometry check keeps operating on the unlevelled, diurnal-corrected field.
The heading-error check is likewise computed on pre-levelling misfits -
levelling would otherwise absorb exactly the signal it looks for.
}
\examples{
res <- run_qc(sim_survey(seed = 2), levelling = FALSE)
lev <- level_ties(res)
lev$rms
}
