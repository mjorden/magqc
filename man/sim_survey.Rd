% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{sim_survey}
\alias{sim_survey}
\title{Simulate an airborne magnetic survey with known defects}
\usage{
sim_survey(
  n_lines = 14,
  line_spacing = 200,
  line_length = 7000,
  tie_spacing = 2000,
  sample_rate = 10,
  ground_speed = 60,
  nominal_clearance = 60,
  origin = c(lon = -108.5, lat = 43.2),
  start_time = as.POSIXct("2024-06-14 08:00:00", tz = "UTC"),
  defects = TRUE,
  seed = 42
)
}
\arguments{
\item{n_lines}{Number of traverse lines.}

\item{line_spacing}{Traverse spacing, m.}

\item{line_length}{Traverse length, m.}

\item{tie_spacing}{Tie-line spacing, m.}

\item{sample_rate}{Magnetometer sample rate, Hz.}

\item{ground_speed}{Ground speed, m/s.}

\item{nominal_clearance}{Terrain clearance, m.}

\item{origin}{\code{c(lon, lat)} of the local grid origin.}

\item{start_time}{POSIXct start of the flight.}

\item{defects}{Inject defects?}

\item{seed}{Random seed.}
}
\value{
A list of class \code{magqc_sim} with elements \code{data} (an
\code{\link[=as_survey]{as_survey()}} tibble), \code{base} (base-station tibble: \code{time},
\code{mag_base}), \code{truth} (tibble of injected defects with the time window
each one occupies), \code{spec} (a \code{\link[=survey_spec]{survey_spec()}} matching the flight
parameters) and \code{origin}.
}
\description{
Generates a block of traverse and tie lines flown over a synthetic
terrain and magnetic field, with a base-station diurnal record, and - when
\code{defects = TRUE} - injects a catalogue of the failures a real acceptance
review is looking for. Every injected defect is recorded in the returned
\code{truth} table, which is what the package tests assert against and what
lets a reader of the report check that each flagged interval is a real
problem rather than a false positive.
}
\details{
Injected defects (all on traverse lines unless stated):
\itemize{
\item isolated spikes of 5-40 nT on random samples
\item a stretch where sensor noise rises to 0.25 nT (fourth difference)
\item a stretch flown 45 m off the planned line
\item one whole line flown 60 m off position (line separation)
\item a stretch flown 35 m too high (terrain clearance)
\item a 12 s dropout in the data stream (gap)
\item a stretch with every second sample missing (along-line spacing)
\item a magnetic-storm excursion on the base station (diurnal)
\item a +2.5 nT bias on southbound lines (heading error, via crossovers)
}
}
\examples{
sim <- sim_survey(seed = 1)
nrow(sim$data)
sim$truth
}
