% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/igrf.R
\name{igrf_field}
\alias{igrf_field}
\title{IGRF main field at survey points}
\usage{
igrf_field(lon, lat, time, altitude = 0, n = 5L)
}
\arguments{
\item{lon, lat}{Longitude and latitude, decimal degrees.}

\item{time}{A POSIXct (or a vector; the mean sets the epoch).}

\item{altitude}{Height above the WGS84 spheroid, metres. The main field
falls by about 0.025 nT per metre, so a rough value is enough.}

\item{n}{Lattice size (\code{n} by \code{n} evaluations).}
}
\value{
A data frame with one row per point: \code{F} (total intensity, nT),
\code{I} (inclination, degrees, positive down), \code{D} (declination, degrees,
positive east); attributes \code{epoch} (decimal year) and \code{model}
(the \code{igrf} package version).
}
\description{
Evaluates the International Geomagnetic Reference Field (via the \code{igrf}
package) on a small longitude/latitude lattice over the points, at one
altitude and epoch, and interpolates total intensity, inclination and
declination bilinearly to each point. Over a survey block the main field
varies by a few nT per kilometre and is smooth, so the lattice is exact
to well under a tenth of a nanotesla and thousands of samples cost the
same as twenty-five model evaluations.
}
\examples{
igrf_field(-108.5, 43.2, as.POSIXct("2024-06-14", tz = "UTC"), altitude = 1600)
}
