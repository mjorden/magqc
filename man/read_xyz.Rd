% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survey.R
\name{read_xyz}
\alias{read_xyz}
\title{Read a Geosoft-style XYZ flight file}
\usage{
read_xyz(
  path,
  columns = NULL,
  col_map = NULL,
  time_origin = as.POSIXct("1970-01-01", tz = "UTC"),
  ...
)
}
\arguments{
\item{path}{Path to the file.}

\item{columns}{Character vector of column names, in file order. Overrides
any header comment.}

\item{col_map}{Named character vector mapping the survey columns this
package needs (\code{x}, \code{y}, \code{mag_raw}, \code{radar_alt}, \code{time}, ...) to the names
used in the file, e.g. \code{c(x = "X", y = "Y", mag_raw = "MAGRAW")}.}

\item{time_origin}{POSIXct date the file's time-of-day column is measured
from. XYZ files usually store seconds since midnight; if the mapped
\code{time} column is already POSIXct this is ignored.}

\item{...}{Passed to \code{\link[=as_survey]{as_survey()}}.}
}
\value{
A \code{magqc_survey} tibble.
}
\description{
Parses the ASCII XYZ interchange format used by most airborne contractors:
comment lines begin with \code{/}, each flight line is introduced by a
\verb{Line <name>} or \verb{Tie <name>} header, and the samples that follow are
whitespace-delimited. Column names are taken from the last comment line
before the first data row unless \code{columns} is given.
}
